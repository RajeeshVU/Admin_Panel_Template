import React, { FC } from 'react';
import Typography from '../../components/typography/Typography';

interface RecordsProps {
  
}

const Records: FC<RecordsProps> = ({}) => {
  return (
    <>
       <Typography tag={'h1'} label={'Records'}/>
    </>
  );
};

export default Records;