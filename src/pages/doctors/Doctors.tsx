import React, { FC } from 'react';
import Typography from '../../components/typography/Typography';

interface DoctorsProps {
  
}

const Doctors: FC<DoctorsProps> = ({}) => {
  return (
    <>
       <Typography tag={'h1'} label={'Dcotors'}/>
    </>
  );
};

export default Doctors;